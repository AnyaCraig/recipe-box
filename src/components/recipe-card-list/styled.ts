import styled from 'styled-components';

export const RecipeUnorderedList = styled.ul`
  list-style: none;
  display: flex;
  justify-content: space-around;
  flex-wrap: wrap;
  border: 3px solid purple;
  padding: 0;
  margin: 0;
`;
